[project]
name = "tenet-cotengra-python"
version = "0.1.0"
description = "Python environment for TeNeT's optional cotengra planner backend."
requires-python = ">=3.10"
dependencies = [
    "cotengra>=0.7,<0.8",
]

[tool.uv]
package = false
